@model IEnumerable<FUMETTERIA_CAP2._0.Models.Genere>

@{
    ViewBag.Title = "Index";
}

<h2 class="bold-text">LISTA GENERI</h2>

<p>
    @Html.ActionLink("aggiungi genere", "Create", null, new { @class = "text-primary bold-text  " })
</p>
<table class="table  table-striped table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.genere1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
           <span class="bold-text card color">@Html.DisplayFor(modelItem => item.genere1)</span> 
        </td>
        <td>
          
            @Html.ActionLink("Elimina", "Delete", new { id=item.IDgenere }, new {@class="text-primary bold-text card color"})
        </td>
    </tr>
}

</table>
